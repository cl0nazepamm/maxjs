/**
 * Reserved Three.js layer index for optional tooling (e.g. future split passes).
 * Kept so index.html and any cached copies can reference a stable symbol.
 */
export const MAXJS_LAYER_SSR_EXCLUDE = 11;
